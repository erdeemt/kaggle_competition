# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Local settings (keep shared .claude/skills tracked)
.claude/settings.local.json

# Yarışma verisi (Kaggle'da /kaggle/input altında; repoda tutulmaz)
*.zarr
*.geff
/data/

# Model ağırlıkları (Kaggle dataset'lerinde tutulur)
*.pth
*.pt
*.ckpt

# Çıktılar — ağır ara tahminler hariç. Figürler repoda tutulur (EDA/rapor).
outputs/predictions/*
!outputs/predictions/.gitkeep
